[project]
license = { text = "AGPL-3.0-or-later" }
name = "roxabi-intel"
version = "0.2.0"
description = "Discord links digest with frontmatter MDs and dynamic gallery"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "discord.py>=2.7.1",
    "jinja2>=3.1.6",
    "tomli>=2.0; python_version < '3.11'",
    "lyra",
]

[tool.uv.sources]
lyra = { git = "https://github.com/Roxabi/lyra.git", branch = "staging" }

[project.optional-dependencies]
dev = ["ruff>=0.15.10"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
# tools/ holds gate scripts vendored verbatim from roxabi-plugins/plugins/dev-core/tools.
# Formatting them here rewrites upstream bytes and recreates the drift ssot-verify V18
# reports, so the copy is excluded rather than reformatted.
extend-exclude = ["tools/"]
# Explicit paths bypass exclude unless force-exclude is set; the pre-commit hook
# passes staged filenames explicitly, so without this the exclusion is inert there.
force-exclude = true

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.ruff.lint.per-file-ignores]
"plugins/web-intel/**/*.py" = ["E402", "E501"]
"services/**/*.py" = ["E501"]

[tool.pyright]
exclude = [
    "plugins/web-intel/",
    "services/",
    ".venv/",
    ".git/",
    "__pycache__/",
    ".ruff_cache/",
]
ignore = ["plugins/web-intel/", "services/"]

[dependency-groups]
dev = [
    "pip-licenses>=5.5.5",
    "pre-commit>=4.5.1",
]
